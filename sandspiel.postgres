CREATE TABLE creations (
    id      char(20) CONSTRAINT firstkey PRIMARY KEY,
    data_id      char(32) NOT NULL,
    title            varchar(220) NOT NULL,
    score         integer NOT NULL,           
    timestamp            timestamp NOT NULL

);

CREATE TABLE votes (
    id      char(20),
    ip      cidr NOT NULL
);

CREATE INDEX ON votes (id, ip);